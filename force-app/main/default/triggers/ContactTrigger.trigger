trigger ContactTrigger on Contact (after insert,before insert,before Update,after Update) {
	//new Triggers()
	//.bind(Triggers.Evt.afterinsert, new ContactTriggerHandler())
    //.bind(Triggers.Evt.afterUpdate, new ContactTriggerHandler())
    //.manage();
}